# Azure OpenAI Configuration (Responses API)
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=gpt-5.2

# Authentication (choose one):
# Option 1: API Key
AZURE_OPENAI_API_KEY=your-api-key-here

# Option 2: Entra ID (Azure AD) - leave AZURE_OPENAI_API_KEY unset
# Uses DefaultAzureCredential (Azure CLI, Managed Identity, etc.)

# Durable Task Scheduler Configuration
# For local emulator (default):
ENDPOINT=http://localhost:8080
TASKHUB=default

# For Azure Durable Task Scheduler:
# ENDPOINT=https://your-scheduler.durabletask.io
# TASKHUB=your-taskhub-name

# Azure Authentication (for deployed scheduler)
# AZURE_MANAGED_IDENTITY_CLIENT_ID=your-managed-identity-client-id

# FastAPI Server Configuration
HOST=0.0.0.0
PORT=8000
